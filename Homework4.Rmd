---
title: "Homework3"
output: 
  html_document: 
    keep_md: yes
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r message=FALSE, warning=FALSE}
library(tidyverse)
```

```{r}
homerange <- readr::read_csv("data/Tamburelloetal_HomeRangeDatabase.csv")
```

```{r}
spec(homerange)
#Allows us to see the full details of the columns
```


Explore the data. Show the dimensions, column names, classes for each variable, and a statistical summary. Keep these as separate code chunks.
```{r}
dim(homerange)
```

```{r}
colnames(homerange)
```

```{r}
str(homerange)
```

```{r}
summary(homerange)
```

Are there NA's in your data? Show the code that you would use to verify this, please.  
```{r}
for i in homerange {
  if (is.na(homerange)) == "TRUE" {
  i += 1
  }
}
print('i')
```


